/*
*) 입력
mirkovC4nizCC44
C4
*) 메모리
string s , ps;
int p,len;

*) 시나리오
len = ps.size();
(s에 ps가 남아있는동안 반복)
    p = s.find(ps); // s에서 ps(C4)의 위치를 찾는다.
    // S의 p위치에서 len만큼 공백으로 replace한다.

*) 출력
(1)
cout << "p :"<<p<<",s:"<<s<<endl;
*/
